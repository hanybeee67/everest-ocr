---
description: 현재 변경 사항을 스테이징, 커밋하고 원격 저장소로 푸시합니다.
---

1. `git status`를 확인하여 변경 사항을 파악합니다.
2. `git add .` 명령어로 모든 변경 사항을 스테이징합니다.
// turbo
3. 변경 내용을 요약하는 적절한 커밋 메시지를 생성하여 `git commit -m "message"`를 실행합니다.
// turbo
4. `git push` 명령어로 원격 저장소에 업로드합니다.
